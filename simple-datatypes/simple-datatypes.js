// Prompt #1 //
/*
Using the variables `first` and `second` create a new variable called 
`organization` whose value is 'Operation Spark'. This should not be hard-coded.
You must use the `first` and `second` variables to create value.
*/
var first = 'Operation';
var second = 'Spark';


// Prompt #2 //
/*
Using += opeator, reassign `city` so that it's new value is 'New Orleans, LA';
*/
var city = 'New Orleans';



// Prompt #3 //
/*
Create a function called `addDescriptor` that takes in a `name` and a
`descriptor`, which will be either 'jr' or 'sr'. This function should return
the input name with descriptor uppercased like so.

// example output
addDescriptor('Stanley Ray', 'jr'); // => 'Stanley Ray, JR'
*/

